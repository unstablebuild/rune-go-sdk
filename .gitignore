# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Example binaries built ad-hoc at the repo root (e.g. `go build ./examples/snippets`)
/snippets

# Test binary, build with `go test -c`
*.test

bin
target

# rune swap files
*.swp
.runerc
go.work
go.work.sum

# macos
.DS_Store

# crash files
rune_crash_report_*
*.db
*.tar.gz
*.rswp
